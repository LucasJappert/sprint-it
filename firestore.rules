rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all operations for now since we're using custom authentication
    match /{document=**} {
      allow read, write: if true;
    }
  }
}